# MCP Tool Visibility E2E Root Rules

Root does nothing interactively for this test. The harness runs the Rust WASM integration assertion directly so failures are deterministic and include a full role/runtime diff table.
